extends ../../layout

block head
  title TransportVic - Tracker/V/Line trips by consist
  
block title
  span V/Line trips by consist
  
//- block style
//-   link(rel='stylesheet' href='/static/css/tracker.css')

block content
  h1 V/Line deployments for #{consist} on #{date}
  for trip in trips
    div.trip
      span ##{trip.runID}: #{trip.departureTime} #{trip.origin} - #{trip.destination}: #{trip.consist.join('-')}
