async function fetchLiveTrips(station, db, departureTime) {
  
}

async function getDepartures(station, db, options={}) {
  let { lookBackwards, departureTime } = options
}

module.exports = {
  fetchLiveTrips,
  getDepartures
}